{#
/**
 * @file entity_tree.html.twig
 * Default theme implementation to present Entity tree data.
 *
 * This template is used when viewing Entity tree pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_entity_tree()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('entity_tree') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
